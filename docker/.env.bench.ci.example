# Bench compose env (generated by .github/workflows/benchmark.yml).
# All values are runtime-injected from GitHub Actions secrets; the keys here
# mirror those in docker/.env.bench.example and docker-compose.bench.yml.
# The actual file is written by the workflow at runtime from ${{ secrets.* }}.
#
# Default image: acautomata/openclaw-docker-cn-im:latest
