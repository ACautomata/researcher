# Daily Memory — 2026-06-05

## Benchmark Design Guide Delivered
为师弟/师妹讲解了 paper-review benchmark 设计的完整流程：
- 整体架构（env.sh / metrics.py / qa.jsonl）
- QA 条目 schema（input_material / gold_answer / rubric / judge 模式）
- 现有 8 个 seed 的测试维度覆盖（S1-S5 各阶段）
- 如何扩展现有 benchmark（按阶段、材料类型、判分方式）
- 评分机制详解（rules judge 关键词匹配 vs agent/LLM judge）
- 首轮 bench 结果 1/8 pass avg 0.096 的原因分析（模板不匹配、关键词太严格）
- 实际操作步骤：从 seed_qa 到 run_smoke.py

## GitHub Code Audit — ACautomata/research-agent
用户给了 7 个疑似代码问题，要求在 GitHub 上确认真实存在的 issue（本地 uncommitted 修复不算）。

按 commit `35e4e0c` 核实后，**5 个真实问题** + **2 个误报**：

### 真实问题（可开 issue）
1. **P0 — minimax provider 补丁不完整**：`bench_reapply_setup` 只用 setdefault 写了 apiKey，缺 baseUrl / api / models。依赖镜像预配置，不同环境启动时会验证失败。
2. **P1 — workspaceOnly=True 未被覆盖**：提交的 env_setup.sh 只处理了 sandbox.mode=off，没有设置 tools.fs.workspaceOnly=False，agent 无法访问材料目录。
3. **P1 — CLI 参数 `--session-key` 未迁移到 `--session-id`**：run_bench.py / judge.py / env.sh 三个文件全部用旧参数，新版 OpenClaw 拒绝。
4. **P1 — AGENTS.md 路由策略 vs benchmark 期望矛盾**：wiki 任务路由到 autoresearch（`sessions_spawn`），但 bench 期望 main 直接输出模板。单次调用模型下子 agent 产出不可靠。
5. **P2 — lossless-claw 插件残留配置**：openclaw.json 中 plugins.entries/installs 引用已不存在的插件，每次日志输出 Config warnings。

### 误报（GitHub 代码已正确处理）
- #2 — `_extract_agent_text` JSON 提取：fallback 到 `_strip_diagnostics` 后行为可接受（无 uncommitted 版中的 `_find_payload_json` 扫描但尚可）。
- #3 — stderr 未检查：提交版本实际已检查 stderr（run_bench.py / env.sh 均有）。

### 额外发现
- 本地运行时有大量 uncommitted fixes 覆盖了上述问题，说明用户/同事已在本地修，尚未 push。
- `run_smoke.py` 是 untracked file，不算 GitHub 问题。
